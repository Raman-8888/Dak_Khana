# Laravel API (see backend/.env APP_URL)
VITE_API_URL=http://localhost:8000

# Supabase (optional): anon key only in the browser. PDF uploads use SERVICE_ROLE on the Laravel backend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
